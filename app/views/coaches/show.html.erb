<p id="notice"><%= notice %></p>

<p>
  <% if can? :read, @coaches %>
  <%= link_to "Back", coaches_path %>
    <% end %>
</p>

<p>
  <strong>Coach Name:</strong>
  <%= @coach.name %>
</p>

<h3> Listing Mentees </h3>
<table class="table table-striped">
  <thead>
  <tr>
    <th>Name</th>
    <th>Email</th>
    <th>Availability</th>
    <th>Prophecy</th>
    <th>Calendar Schedule</th>
  </tr>
  </thead>

  <tbody>
  <% @coach.mentees.each do |mentee| %>
    <tr>
      <td><%= link_to mentee.name, coach_mentee_path(@coach, mentee) %></td>
      <td><%= mentee.email %></td>
      <td><%= mentee.availability %></td>
      <td><%= mentee.prophecy %></td>
      <td><%= link_to "View", mentee_events_path(mentee) %></td>
    </tr>
  <% end %>
  </tbody>
</table>
