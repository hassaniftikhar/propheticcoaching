<p id="notice"><%= notice %></p>

<p>
  <strong>Body:</strong>
  <%= @exercise.body %>
  <div>
	  <strong>
	  	Category:
	  </strong>
	  <%= @exercise.categories.order("name asc").collect {|category| link_to("#{category.name}", category_path(category)) }.join(", ").html_safe %>
		</div>
</p>


<%= link_to 'Edit', edit_exercise_path(@exercise) %> |
<%= link_to 'Back', exercises_path %>
