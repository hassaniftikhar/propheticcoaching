<%= form_for @email_history||=EmailHistory.new, :url => user_mentee_email_histories_path(coach, mentee) do |f| %>
  <% if @email_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@email_history.errors.count, "error") %> prohibited this email_history from being saved:</h2>

      <ul>
      <% @email_history.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
