<h3> Email History </h3>
<table class="table table-striped" >
  <thead>
  <tr>
    <th></th>
  </tr>
  </thead>

  <tbody>
  <% @email_histories.each do |email_history| %>
    <tr>
      <td><%= email_history.body %></td>
      <td><%= email_history.created_at.strftime("%d %b, %Y %I:%M %p") %></td>
    </tr>
  <% end %>
  </tbody>
</table>
<%= paginate @email_histories %>