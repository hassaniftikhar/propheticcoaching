<%= form_for @accomplishment||=Accomplishment.new, :url => user_mentee_accomplishments_path(coach, mentee) do |f| %>
  <% if @accomplishment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@accomplishment.errors.count, "error") %> prohibited this accomplishment from being saved:</h2>

      <ul>
      <% @accomplishment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_area :body %>
  </div>

  <div class="actions">
        <%= label_tag "Send Email" %>
        <%= check_box_tag("is_send_email","1", true,  {}) %>
        <br/><br/><br/>
        <%= f.submit "Create" %>
  </div>
<% end %>
