<!DOCTYPE html>
<html>
<head>
  <title>Propheticcoaching</title>
  <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
  <%= include_gon %>
</head>
<body>

<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
      <a class="brand" href="/">Prophetic Coaching</a>

      <div class="nav-collapse">
        <ul class="nav">
          <% if current_user %>
            <li><%= link_to "Admin Panel", admin_users_path if current_user.has_role?(:admin) %></li>
            <li><%= link_to "My Mentees List", coach_path(current_user) %></li>
            <li><%= link_to "View Coaches Schedule", events_path(current_user) %></li>
            <li><%= link_to "Search Ebooks", ebooks_path %></li>
            <li><%= link_to "Logout", destroy_user_session_path, :method => :delete %></li>
            <li> <%= "Welcome #{current_user.name}" %> </li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
</div>

<div class="container">
  <% flash.each do |name, msg| %>
    <% if msg.is_a?(String) %>
      <div class="alert text-center alert-<%= name == :notice ? "success" : "error" %>">
        <a class="close" data-dismiss="alert">&#215;</a>
        <%= content_tag :div, msg.html_safe, :id => "flash_#{name}" %>
      </div>
    <% end %>
  <% end %>
  <div class="row">
    <%= subscribe_to "/chats/talk" %>
    <%= subscribe_to "/chats/talk/#{current_user.escaped_name+'_'+current_user.id.to_s}" if current_user %>
    <div class="span1"></div>
    <div class="span9"><%= yield %></div>
  </div>
</div>

<% if current_user %>
  <div id="chat-main">
    <div id="chat_div" user_id="<%= current_user.id %>"></div>

    <div id="chat_div2" class="online-clients" style="border-radius: 5px; ">
      <div class="header" id="name_header" style="background: gray">Online Contacts</div>
      <div id="online_contacts">
      </div>
    </div>
  </div>
<% end %>

<script type="text/javascript">
  authenticity_token = "<%= form_authenticity_token %>"
</script>


</body>
</html>
