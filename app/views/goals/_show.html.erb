<h3> Listing Goals </h3>
<table class="table table-striped" >
  <thead>
  <tr>
    <th></th>
  </tr>
  </thead>

  <tbody>
  <% @goals.order("id DESC").each do |goal| %>
    <tr>
      <td><%= goal.body %></td>
    </tr>
  <% end %>
  </tbody>
</table>
<%= paginate @goals %>